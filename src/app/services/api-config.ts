// Point this at your TeqetariApi instance. In a real project, wire this up to
// Angular's environment.ts (environment.apiBaseUrl) instead of hardcoding it.
export const API_BASE_URL = 'http://localhost:5134/api';
